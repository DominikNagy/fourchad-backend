spring:
  datasource:
    url: jdbc:mysql://localhost:3308/fourchad
    username: localuser
    password: localpass
  jpa:
    hibernate:
      ddl-auto: update
      properties:
        hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
